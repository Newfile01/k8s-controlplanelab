---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: controlplanetests.controlplane.lab.local
spec:
  group: controlplane.lab.local
  names:
    kind: ControlPlaneTest
    listKind: ControlPlaneTestList
    plural: controlplanetests
    singular: controlplanetest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ControlPlaneTest is the Schema for the controlplanetests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ControlPlaneTest
            properties:
              apiServerEtcdStress:
                properties:
                  aggressiveReconcile:
                    type: boolean
                  burst:
                    format: int32
                    type: integer
                  enabled:
                    type: boolean
                  frequentStatusUpdates:
                    type: boolean
                  qps:
                    format: int32
                    type: integer
                  reconcileRequeueDelay:
                    description: Nombre de millisecondes avant requeue
                    format: int32
                    type: integer
                  recreateRequeueDelay:
                    description: Nombre de millisecondes avant requeue
                    format: int32
                    type: integer
                  recreateResources:
                    type: boolean
                type: object
              configMapCount:
                description: ConfigMaps & Secrets
                format: int32
                type: integer
              configMapSizeKB:
                format: int32
                type: integer
              containerName:
                description: |-
                  ============================================================
                  WORKLOAD DEFINITION
                  ============================================================
                type: string
              controllerManagerStress:
                properties:
                  aggressiveGarbageCollection:
                    type: boolean
                  enabled:
                    type: boolean
                  garbageCollectionDelay:
                    format: int32
                    type: integer
                  recreateReplicaSets:
                    type: boolean
                  replicaRecreateDelay:
                    format: int32
                    type: integer
                type: object
              customLabel:
                description: |-
                  ControlPlaneTestSpec defines the desired state of ControlPlaneTest
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  The following markers will use OpenAPI v3 schema to validate the value
                  More info: https://book.kubebuilder.io/reference/markers/crd-validation.html
                properties:
                  key:
                    type: string
                  value:
                    type: string
                type: object
              deploymentCount:
                description: Nombre de Deployments à générer.
                format: int32
                type: integer
              image:
                description: Image utilisée pour les Pods générés.
                type: string
              operatorStress:
                properties:
                  enabled:
                    type: boolean
                  informer:
                    properties:
                      watchConfigMaps:
                        type: boolean
                      watchDeployments:
                        type: boolean
                      watchPods:
                        type: boolean
                    type: object
                  profile:
                    type: string
                  reconcile:
                    properties:
                      baseDelaySeconds:
                        description: Délai (avant réconciliation ?)
                        format: int32
                        type: integer
                      burst:
                        description: Explosion/Pic de requêtes recevable
                        format: int32
                        type: integer
                      maxConcurrent:
                        description: Nbre réconciliations simultanées possibles
                        format: int32
                        type: integer
                      maxDelaySeconds:
                        description: Délai maximum avant nouvelle réconciliation
                        format: int32
                        type: integer
                      qps:
                        description: QPS autorisées pour le pod operator_controller
                        format: int32
                        type: integer
                    type: object
                type: object
              podLifecycleStorm:
                properties:
                  crashLoopSimulation:
                    type: boolean
                  deletePodsDelay:
                    format: int32
                    type: integer
                  deletePodsRandomly:
                    type: boolean
                  enabled:
                    type: boolean
                type: object
              replicasPerDeployment:
                description: Nombre de Pods par Deployment.
                format: int32
                type: integer
              resourcesPerPod:
                properties:
                  cpuLimit:
                    description: Max autorisé à Kubernetes
                    type: string
                  cpuRequest:
                    description: Ressources demandées
                    type: string
                  memoryLimit:
                    type: string
                  memoryRequest:
                    type: string
                type: object
              schedulerStress:
                properties:
                  affinityMode:
                    description: |-
                      Valeurs possibles pour AffinityMode et AntiAffinityMode :
                      "soft" = PreferredDuringSchedulingIgnoredDuringExecution
                      "hard" = RequiredDuringSchedulingIgnoredDuringExecution

                      Utilisé pour forcer le regroupement avec autres pods (par label)
                    type: string
                  affinitySelector:
                    description: |-
                      Paramètres influençant le comportement du Scheduler Kubernetes.

                      Ces paramètres ne modifient PAS la forme de la charge.
                      Ils influencent uniquement le placement des Pods
                      sur les nœuds du cluster.
                    properties:
                      key:
                        type: string
                      value:
                        type: string
                    type: object
                  antiAffinityMode:
                    description: PodAntiAffinity, forcera la séparation des pods selon
                      un critère donné
                    type: string
                  antiAffinitySelector:
                    description: |-
                      Paramètres influençant le comportement du Scheduler Kubernetes.

                      Ces paramètres ne modifient PAS la forme de la charge.
                      Ils influencent uniquement le placement des Pods
                      sur les nœuds du cluster.
                    properties:
                      key:
                        type: string
                      value:
                        type: string
                    type: object
                  enabled:
                    description: Active le scénario Scheduler Stress.
                    type: boolean
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      Nombre de nœuds ciblés par le scénario, utilisés par certaines stratégies de placement. En attente d'upgrade future
                      NodeCount int32 `json:"nodeCount,omitempty"`
                      Sélection stricte des nœuds via labels Kubernetes.
                    type: object
                  topologySpread:
                    description: Répartition homogène des Pods = équilibrage
                    type: boolean
                type: object
              secretCount:
                format: int32
                type: integer
              secretSizeKB:
                format: int32
                type: integer
              servicePort:
                description: Définition des ports d'accès et cible de l'application
                  déployé via la CR
                format: int32
                type: integer
              targetPort:
                format: int32
                type: integer
            required:
            - containerName
            - deploymentCount
            - image
            type: object
          status:
            description: status defines the observed state of ControlPlaneTest
            properties:
              availableReplicas:
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configMapNames:
                items:
                  type: string
                type: array
              deploymentNames:
                items:
                  type: string
                type: array
              lastStatusUpdate:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              readyReplicas:
                format: int32
                type: integer
              secretNames:
                items:
                  type: string
                type: array
              serviceName:
                type: string
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
